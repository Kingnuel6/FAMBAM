Add a soft piano track here named `piano.mp3` to enable the optional background
music on Scene 1 (the envelope landing). The unmute toggle and playback logic
already reference `/audio/piano.mp3` — until a file is added, playback fails
silently and the site is unaffected.
